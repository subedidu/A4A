//import breakfast from '../json_data/breakfast.json';
import { Component, Input } from '@angular/core';

@Component({
    selector: 'lunch',
    template: `<p class="footer">{{Name}}  /  {{Student_Number}}  /  {{Login_Name}}  /  {{Campus}}</p>`,
})

export class LunchComponent {
    
}
